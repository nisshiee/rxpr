name: Publish

on:
  push:
    tags:
      - v*

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Publish
        run: cargo publish --token ${{ secrets.CARGO_TOKEN }}
